export class FacultyDto {
  constructor(public startDateEducation: Date, public countYearEducation: number, public studentId: number,
              public groupId: number, public curatorId: number, public id: number) {
  }
}
