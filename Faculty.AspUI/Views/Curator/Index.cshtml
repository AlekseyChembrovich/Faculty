@model IList<Faculty.DataAccessLayer.Models.Curator>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Student - List";
}

<div class="container mb-3">
    @Html.ActionLink("New", "Create", "Curator", null, new { @class = "btn btn-primary" })
</div>

<table class="table">
    <thead>
        <tr>
            <th scope="col">Number</th>
            <th scope="col">Surname</th>
            <th scope="col">Name</th>
            <th scope="col">Doublename</th>
            <th scope="col">Phone</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @for (var i = 0; i < Model.Count; i++)
        {
            <tr>
                <td>@(i + 1)</td>
                <td>@Model[i].Surname</td>
                <td>@Model[i].Name</td>
                <td>@Model[i].Doublename</td>
                <td>@Model[i].Phone</td>
                <td>
                    @Html.ActionLink("Delete", "Delete", "Curator", new { id = Model[i].Id }, new { @class = "btn btn-primary mr-2" })
                    @Html.ActionLink("Edit", "Edit", "Curator", new { id = Model[i].Id }, new { @class = "btn btn-primary" })
                </td>
            </tr>
        }
    </tbody>
</table>